# 에이전트 규칙 — dc_01 트래커

2026 미국 중간선거 × 데이터센터 정치 리스크 트래커.
이 저장소에서 작업할 때 아래 규칙은 예외 없이 적용된다.

## 프로젝트가 하는 일

후보의 데이터센터 정책 입장을 **방향 × 구속력 × 범위 × 직위권한**으로 분해해
실제 규제 영향력(레버리지)을 계산한다. 선거 광고에서 가장 세게 말하는 후보와
실제로 규제에 서명한 후보가 다르다는 것이 핵심 발견이다.

## 절대 원칙 (위반 시 작업 중단)

### P1. 사실과 분석을 섞지 않는다
선거 일정·후보·발언·법령은 Fact. 점수와 가중치는 분석.
파생 테이블은 `data_type='AI Analysis'` 가 CHECK 로 강제된다. 우회하지 마라.

### P2. 모르는 값은 0이 아니다
`NULL` + `status_code` 로 남긴다. 추정해서 채우지 마라.
빈칸이 많은 것은 결함이 아니라 이 프로젝트의 설계다.

### P3. 근거 없이는 점수를 넣을 수 없다
`position_axis_score` 입력은 `position_evidence` 최소 1건을 요구한다.
DB 트리거가 막는다. **트리거를 비활성화하거나 우회하는 코드를 쓰지 마라.**
`evidence_type` 에 "정당 소속"은 존재하지 않는다. 정당으로 입장을 추론하지 마라.

### P4. 정의가 다른 수치를 합치지 않는다
"미국 데이터센터 수"는 출처별로 1,208~7,759로 최대 6배 차이 난다.
벤더별로 나눠 저장하고 벤더 내부에서만 비교한다.

## 코드 규칙

### 기존 스크립트를 수정하지 마라
날짜별 파일을 새로 만든다. `scripts/NN_update_MMDD.py` 형식.
`race_rating` 등은 날짜별 이력이 쌓여야 변화 추적이 된다.
기존 파일을 고치면 이전 스냅샷이 사라진다.

예외는 명백한 버그 수정(잘못된 source_id 참조 등)뿐이며, 이때도 먼저 보고한다.

### 실행 순서는 매니페스트를 따른다
`scripts/pipeline.txt` 가 실행 순서다. 파일명 알파벳순과 다르다.
새 갱신 스크립트는 `# --- 이하 빌드` 주석 **앞**에 추가한다.

```bash
./scripts/run_pipeline.sh       # DB 삭제 후 전체 재현
python3 scripts/99_validate.py  # 무결성 검증
```

### 커밋 전 반드시
1. `./scripts/run_pipeline.sh` 가 끝까지 성공
2. `python3 scripts/99_validate.py` 가 exit 0
3. 하나라도 실패하면 커밋하지 말고 사용자에게 보고

## 데이터 적재 기준

적재해도 되는 것
- 필드 기간(시작·종료)이 명시된 여론조사
- 기준일이 특정된 레이스 등급
- 원문 URL과 발행일이 있는 정치인 발언·법안·행정명령

적재하면 안 되는 것
- 날짜가 없는 조사나 등급
- 후보 명단에 없는 인물이 등장하는 조사
- 2차 인용만 있고 원문을 확인하지 못한 수치
- 여러 출처를 직접 평균 낸 자체 계산값
  (집계기관의 공식 평균은 `is_average=1` 로 적재 가능)

## 확인된 함정 (반복 발생)

1. **집계 사이트 캐시 오염** — RealClearPolling 발췌에 2024년 대진
   (Brown vs Moreno, McCormick vs Casey, Hovde vs Baldwin, Rogers vs Slotkin)이
   2026년 목록으로 섞여 나온다. 2회 발생. 연도와 후보명을 반드시 대조하라.
2. **동명이인** — Chris Sununu(전 주지사) ≠ John E. Sununu(전 상원의원, 실제 후보).
   Dan S. Sullivan(현직) ≠ Daniel J. Sullivan(동명 출마자).
3. **실존하지 않는 대진** — 조지아에서 "Kemp vs Ossoff" 조사가 유통되나
   Kemp는 후보가 아니다. 실제 대진은 Mike Collins vs Ossoff.
4. **후보 교체 누락** — 메인 상원 민주당 후보가 사퇴·교체됐는데 5주간 반영이 안 됐다.
   갱신할 때마다 후보 상태를 재확인하라.
5. **구속력 과소평가** — "발표 예정"으로 적은 것이 이미 성립된 법률인 경우가 있었다
   (Kotek, 0.25 → 1.00). 새 근거를 볼 때 기존 기록의 구속력도 다시 보라.

## 출처 우선순위

Tier 1 공식 선거기관 → 2 주정부 → 3 미국 의회 → 4 후보 공식 → 5 법안 DB
→ 6 공공기관·대학 → 7 기업 공식발표 → 8 산업·선거 리서치 → 9 주요 언론

**지역 언론이 전국 언론보다 유용하다.** 후보의 구체적 정책 문구는 주 단위 매체에만 있다.

## 브라우저 사용

원문 확인이 목적이다. 검색 결과 요약만 보고 적재하지 마라.
유료벽이나 robots 차단으로 원문을 못 보면, 적재하지 말고 그 사실을 보고하라.

## 보고 형식

- 적재한 항목 수 (조사 / 등급 / 근거)
- **적재하지 않은 항목과 그 이유** ← 이게 더 중요하다
- 파이프라인·검증 통과 여부
- 기존 기록 중 수정이 필요해 보이는 것
