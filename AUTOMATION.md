# 자동화

## 0. 지금 바로 할 일 (순서대로)

```
[1] 저장소에 푸시            ← .agents/ 와 AGENTS.md 가 아직 GitHub 에 없다
[2] Antigravity 설치·저장소 열기
[3] 기준선 확인 (명령 2개)
[4] 프로젝트 설정 (경로 제한 · RCP 차단 · 계획 검토 모드)
[5] 수동 1회 실행 (커밋 금지)
[6] 체크포인트 4개 확인
[7] 스케줄 등록
[8] 2~3주 뒤 자동 커밋 허용
```

각 단계 상세는 파트 1 참조. 건너뛰지 말아야 할 것은 [3]과 [5]다.

---

이 저장소는 두 가지 자동화를 함께 쓴다. 역할이 다르다.

| | GitHub Actions | Antigravity 에이전트 |
|---|---|---|
| 하는 일 | 재현·검증·재빌드 | 새 데이터 수집·적재 |
| 판단 | 없음. 정해진 명령만 | 계획·검색·원문 확인 |
| 위험 | 낮음 | 모델이 사실을 잘못 판단할 수 있음 |
| 주기 | 매일 자동 | 주 1~2회 또는 수동 |

에이전트가 수집하고, Actions 가 그 결과를 독립적으로 재현·검증한다. 이중 확인이다.

---

# 파트 1 — Antigravity 에이전트


## 왜 Antigravity인가 (그리고 어디가 위험한가)

GitHub Actions는 정해진 명령만 반복한다. 그래서 앞서 일일 워크플로에서
"새 데이터 수집"을 뺐다. 모델이 사실을 판단하는 지점이 이 프로젝트에서
가장 자주 틀린 부분이었기 때문이다.

Antigravity 에이전트는 계획을 세우고 코드를 쓰고 터미널을 돌리고
브라우저로 자기 작업을 검증한다. 그래서 수집을 실제로 맡길 수 있다.

**대신 위험이 그대로 들어온다.** 에이전트가 그럴듯한 숫자를 만들어
커밋하면 공개 사이트에 그대로 반영된다. 방어는 세 겹으로 건다.

| 층 | 장치 | 막는 것 |
|---|---|---|
| 지시 | `AGENTS.md` | 정당 추론, 날짜 없는 조사, 기존 스크립트 수정 |
| 코드 | DB 트리거 | 근거 없는 점수 입력 |
| 검증 | `99_validate.py` | 고아 참조, 미래 날짜, 범위 이탈 |

세 번째가 마지막 방어선이다. 커밋 전에 반드시 통과해야 한다.

---

## 커스터마이징 4개 층

| 층 | 성격 | 위치 | 이 프로젝트 |
|---|---|---|---|
| Rule | 상시 로드되는 컨텍스트 | `AGENTS.md`, `.agents/rules/` | 절대 원칙 P1~P4, 함정 목록 |
| Skill | 설명으로 발견, 관련될 때만 전문 로드 | `.agents/skills/<이름>/SKILL.md` | poll-update, policy-update |
| Workflow | 반복 작업의 구조화된 단계 | `.agents/workflows/` | weekly-tracker-update |
| Agent | 자체 모델·권한·MCP를 가진 명명 에이전트 | `agent.md` | 필요 시 |

Skill은 3단계 점진적 공개를 따른다. 세션 시작 시 이름과 설명만 보이고,
작업이 관련돼 보이면 SKILL.md 전문을 읽고, 그다음 실행한다.

**그래서 `description` 이 가장 중요하다.** 트리거 문구를 다 넣어야 발동한다.
이 저장소의 두 스킬은 '여론조사 갱신', '정책 갱신', '격전지 조사 확인',
'레버리지 갱신' 등을 명시해뒀다.

---

## 단계

### 1. 설치와 저장소 열기

Antigravity를 설치하고 `uouohmk/dc_01` 을 클론해 워크스페이스로 연다.
IDE 버전이 개발 작업에 권장되고, 스탠드얼론 앱은 여러 에이전트를 병렬로
관리하고 스케줄 작업을 돌리는 용도다. 둘 다 써도 된다.

```bash
git clone https://github.com/uouohmk/dc_01.git
cd dc_01
python3 -c "import PIL" || pip install pillow
sudo apt-get install -y fonts-noto-cjk   # 카드뉴스 생성에 필요
./scripts/run_pipeline.sh && python3 scripts/99_validate.py
```

이 두 명령이 통과하지 않으면 에이전트를 붙이지 마라. 기준선이 깨진 상태다.

### 2. 규칙 확인

`AGENTS.md` 가 루트에 있으면 Antigravity가 Rule로 읽는다. 이미 커밋돼 있다.

내용을 한 번 읽어보고, 특히 "확인된 함정" 다섯 개가 맞는지 확인한다.
이건 실제로 겪은 것들이라 에이전트에게 가장 값진 부분이다.

### 3. 프로젝트 설정

설정에서 접근 가능한 파일 경로, 허용·차단 URL, 사용 가능한 MCP 도구를
프로젝트 단위로 지정할 수 있다. 전역 MCP 서버가 전부 노출되는 걸 막는 용도다.

권장 설정:

- **파일 경로**: 저장소 폴더로 제한
- **차단 URL**: `realclearpolling.com` — 2024년 대진 캐시가 2026년 목록에
  섞여 나오는 사고가 두 번 있었다. 차단이 과하면 최소한 AGENTS.md의 대조 규칙을
  따르게 한다
- **MCP**: 이 작업에 불필요한 서버는 전부 끈다

`implementation_plan.md` 같은 산출물을 생성하게 하고 **실행 전에 검토**하도록
설정해두면 에이전트가 계획만 세우고 멈춘다. 처음 몇 주는 이 모드를 권한다.

### 4. 첫 실행 — 수동

스케줄을 걸기 전에 반드시 손으로 한 번 돌린다.

```
.agents/workflows/weekly-tracker-update.md 워크플로를 실행해줘.
이번에는 커밋하지 말고 변경 내용만 보여줘.
```

확인할 것:
- 새로 만든 스크립트가 `scripts/NN_*.py` 형식인가
- 기존 스크립트를 건드리지 않았는가
- `pipeline.txt` 의 빌드 단계 **앞**에 추가했는가
- 거부한 항목을 보고했는가 ← 이걸 보고하지 않으면 규칙을 안 읽은 것이다

### 5. 스케줄 등록

Antigravity 2.0은 스케줄 작업을 지원한다. 스탠드얼론 앱에서 등록한다.

권장 주기:

| 작업 | 주기 | 프롬프트 |
|---|---|---|
| 후보 명단 점검 | 주 1회 | "격전지 후보 사퇴·교체를 확인하고 DB와 대조해줘" |
| 여론조사 갱신 | 주 2회 | "poll-update 스킬로 STALE/AGING 레이스를 갱신해줘" |
| 정책 입장 갱신 | 주 1회 | "policy-update 스킬로 새 행정명령·법안을 확인해줘" |
| 전체 갱신 | 주 1회 | "weekly-tracker-update 워크플로 실행" |

선거일(11/3) 30일 전부터는 여론조사를 일 1회로 올린다.

### 6. 커밋 권한

처음에는 **커밋을 사람이 한다.** 에이전트는 변경만 만들고 멈춘다.
2~3주 돌려보고 거부 판단이 정확하면 그때 자동 커밋을 허용한다.

자동 커밋을 켤 때도 GitHub Actions의 `daily_rebuild.yml` 은 유지한다.
에이전트가 푸시한 결과를 독립적으로 재현·검증하는 이중 확인이 된다.

### 7. agy CLI (선택)

터미널에서 직접 호출할 수 있다. cron이나 다른 스케줄러와 붙이려면 이쪽이 편하다.

```bash
agy "poll-update 스킬로 오하이오·미시간·텍사스·알래스카 조사를 갱신하고,
     검증 통과하면 커밋까지 해줘"
```

---

## 프롬프트 예시

### 여론조사만
```
poll-update 스킬을 써서 여론조사를 갱신해줘.
v_poll_coverage 에서 STALE 과 AGING 인 레이스를 우선하고,
필드 기간이 없는 조사는 적재하지 마.
거부한 항목은 개수와 이유를 알려줘.
```

### 정책 입장만
```
policy-update 스킬로 데이터센터 정책 변동을 확인해줘.
주지사의 행정명령과 서명된 법률을 우선으로 찾고,
기존 기록 중 구속력이 과소평가된 게 있는지도 같이 봐줘.
```

### 특정 주
```
오하이오 상원·주지사 레이스만 갱신해줘.
Brown 과 Husted 의 데이터센터 관련 새 발언이나 법안이 있는지 확인하고,
있으면 근거와 구속력·범위 코드까지 기록해줘.
```

### 점검만
```
커밋하지 말고 점검만 해줘.
후보 명단이 현재 대진과 맞는지, 노후 조사가 몇 건인지,
검증에서 걸리는 게 있는지 보고해줘.
```

---

## 실패 시나리오와 대응

| 증상 | 원인 | 대응 |
|---|---|---|
| 파이프라인이 중간에 죽음 | 새 스크립트가 없는 컬럼·source_id 참조 | 에이전트에게 `99_validate.py` 출력을 주고 수정 요청 |
| 검증에서 고아 참조 | source 등록 없이 evidence 적재 | 실제로 발생했던 오류다. source_id 를 확인시킨다 |
| 같은 조사가 중복 적재 | 이전 갱신과 대조 안 함 | 프롬프트에 "기존 polling 테이블과 대조" 추가 |
| 빈칸을 추정으로 채움 | P2 위반 | AGENTS.md 재확인. 반복되면 해당 커밋 되돌림 |
| 등급을 날짜 없이 적재 | 거부 기준 무시 | 해당 항목 삭제하고 스킬 재로드 |

---

## 한계

에이전트가 자동으로 잘 못하는 것이 있다.

**구속력 판단이 어렵다.** "발표했다"와 "서명했다"의 차이는 기사 문장을
꼼꼼히 읽어야 나온다. Kotek 건에서 실제로 0.25로 잘못 넣었다가
1.00으로 고쳤다. 레버리지 순위가 바뀌면 사람이 근거를 직접 확인하는 게 좋다.

**축이 반대 방향인 후보를 놓치기 쉽다.** Paxton처럼 전력축만 양수인 경우,
요약만 읽으면 "규제 성향"으로 뭉뚱그려진다. 5축을 개별로 채우라고
명시적으로 요구해야 한다.

**미완성 영역은 자동화 대상이 아니다.** `dc_project`, `dc_election_risk`,
`scenario` 는 아직 비어 있다. 이건 설계 판단이 필요한 작업이라
에이전트에게 통째로 맡기지 말고 사람이 STEP 4부터 직접 채우는 게 낫다.


---

# 파트 2 — GitHub Actions


업로드하신 워크플로 두 개를 검토·수정한 결과입니다.

## 원본에서 발견된 문제

| # | 문제 | 결과 | 조치 |
|---|---|---|---|
| 1 | `for f in scripts/0*.py scripts/1*.py` 글롭 정렬 | **첫 실행에서 실패.** `03_build_dashboard.py`가 `04_positions.py`보다 먼저 돌아 `v_effective_position` 미존재 오류 | `scripts/pipeline.txt` 매니페스트 + `run_pipeline.sh` |
| 2 | `cron_auto_collector.py`, `auto_update_poll.py` 부재 | 워크플로가 없는 파일을 호출 | `auto_update_poll.py` 작성. 일일 수집기는 폐기(아래 참조) |
| 3 | `"${{ github.event.inputs.poll_data }}"`를 run 블록에 직접 삽입 | **명령어 주입.** 입력에 `" ; rm -rf . ; "`를 넣으면 실행됨 | `env:`로 전달 |
| 4 | `permissions` 미선언 | 기본 read-only 저장소에서 push 실패 | `contents: write` 명시 |
| 5 | 한글 폰트 미설치 | `05_cards.py`가 ubuntu-latest에서 폰트 못 찾고 실패 | `fonts-noto-cjk` 설치 단계 추가 |

## 설계상 더 큰 문제

원본은 **모델이 파이썬 코드를 생성 → 즉시 실행 → main에 자동 푸시**하는 구조입니다.

이건 이 프로젝트의 존재 이유와 충돌합니다. P3(근거 없이는 점수 입력 금지)를
DB 트리거로 강제해놨는데, 트리거를 우회하는 코드를 모델이 생성해서 실행하면
방어선이 무의미해집니다. 사람 검토 없이 공개 사이트에 반영되는 것도 위험합니다.

### 바꾼 구조

```
[수동 갱신]  원문 입력 → 모델은 JSON만 출력 → 스키마 검증 → 갱신 스크립트 생성
             → 클린 재현 → 무결성 검증 → PR 생성 → 사람이 머지
[일일 실행]  클린 재현 + 무결성 검증 + 날짜 의존 지표 갱신 → 커밋
             (새 데이터 수집 안 함)
```

모델은 코드를 짜지 않고 **JSON만** 내놓습니다. 스키마 검증을 통과한 항목만
정해진 템플릿으로 스크립트가 됩니다.

## 파일

| 경로 | 역할 |
|---|---|
| `.github/workflows/update_tracker.yml` | 수동 갱신 → PR |
| `.github/workflows/daily_rebuild.yml` | 일일 재현·검증 |
| `scripts/pipeline.txt` | 실행 순서 매니페스트 |
| `scripts/run_pipeline.sh` | 매니페스트대로 클린 재현 |
| `scripts/auto_update_poll.py` | JSON 추출 + 검증 + 스크립트 생성 |
| `scripts/99_validate.py` | 무결성 검증. 위반 시 exit 1 |

## 거부 조건

`auto_update_poll.py`는 아래 중 하나라도 걸리면 적재하지 않습니다.

- 출처 URL 없음 / 발행일 형식 오류 / 미래 날짜
- 여론조사에 필드 종료일 없음
- 등급에 기준일 없음 또는 코드 부적합
- **후보명이 DB의 현재 후보 명단에 없음** ← 실존하지 않는 대진 차단
- 모델이 유효한 JSON을 못 내놓음

마지막 항목이 중요합니다. 조지아에서 "Kemp vs Ossoff" 조사가 유통되는데
Kemp는 상원 후보가 아닙니다. 명단 대조로 이런 건이 걸러집니다.

## 99_validate.py 가 검사하는 것

P3 근거 없는 점수 · 출처 없는 근거 · URL 없는 출처 · 날짜 없는 근거
P1 파생 테이블의 Fact 표기 · 근거 없는 시나리오
날짜 미래 조사 · 날짜 없는 등급 · 필드 기간 없는 조사
후보 없는 레이스의 조사 · 점수 범위 이탈 · 고아 참조 4종
P4 복수 벤더 혼재 경고 · 미착수 핵심 테이블 경고

실제로 이 검증기가 기존 데이터의 오류를 하나 잡았습니다.
Husted 법안 발의 근거가 존재하지 않는 `source_id=13`을 참조하고 있었습니다.

## 설정

1. 저장소 Settings → Secrets and variables → Actions → `GEMINI_API_KEY` 등록
2. Settings → Actions → General → Workflow permissions → **Read and write** 선택
3. Settings → Actions → General → **Allow GitHub Actions to create and approve pull requests** 체크

## 사용

**수동 갱신** — Actions 탭 → Manual Update → Run workflow →
출처 URL, 발행일, 원문 텍스트 입력 → PR 생성됨 → 검토 후 머지

**일일 실행** — 매일 KST 09:00 자동. 실패하면 이슈가 자동 생성됩니다.

## 남는 한계

일일 워크플로는 **새 데이터를 수집하지 않습니다.** 원본이 의도한 완전 자동
수집을 하려면 모델이 웹을 검색해 사실을 판단해야 하는데, 이 프로젝트에서
가장 자주 틀린 부분이 바로 그 판단이었습니다.

실제로 겪은 것들입니다. 집계 사이트에 이전 사이클 대진이 섞여 나왔고,
존재하지 않는 대진 조사가 유통됐고, 후보 교체를 5주간 놓쳤고,
같은 지표가 출처별로 6배 차이 났습니다.

사람이 원문 링크를 확인하고 붙여넣는 5분이 이 오류들을 대부분 막습니다.
완전 자동화는 STEP 4~7이 채워지고 데이터 형태가 안정된 뒤에 다시 보시길 권합니다.
